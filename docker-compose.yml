version: '3.8'

services:
  coin-flip-api:
    build: .
    container_name: coin-flip-api
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DEBUG=False
      - TZ=UTC
    restart: unless-stopped
